var mercury = require('mercury')


module.exports = {
    renderAs: 'image'
    location: { renderAs: 'edge', objects: { renderAs: 'image' } },
    memberships: { renderAs: 'edge' },
    children []
};